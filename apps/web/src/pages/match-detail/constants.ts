/**
 * Re-export shared constants for backward compatibility.
 * New code should import directly from '../../utils/constants'.
 */
export { CAN_EDIT_ROLES, EVENT_TYPE_MAP, POSITION_MAP, STATUS_MAP } from '../../utils/constants';
