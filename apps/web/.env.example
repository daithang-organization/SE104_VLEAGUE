# Backend API URL (must include /api prefix)
VITE_API_BASE_URL=http://localhost:8080/api

# Sentry DSN (optional — leave empty to disable error tracking)
VITE_SENTRY_DSN=

# App version for Sentry releases (optional)
VITE_APP_VERSION=dev
